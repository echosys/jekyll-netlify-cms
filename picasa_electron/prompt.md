


//20260318
    update the features file and done vs td md file as you go, check the z_progress folder, add a file for current date if not exist, else modify it to reflect things done for the day. 


//20260321
    treat .app as one file, photo grid 
    reduce size of image name to 1/2, size and date should be 1/3 size, also can not scroll to see more images, also fix the left hand side icons, the text button looks are off

//20260322  
    scroll, timeline group jump 
    preview window display 
    display size 
    divider resize, limit 40%, save size 
    photo navi icon, order not same as display order 
    folder hierachy, collpse affect all subfolders 
        folder 1  . tab, file1, 
        have a dir list tree like line linking all 
        also for BU view
    backup features 
        restore, compare list, folder name save, match enforce


# 24 too new for sqlite3
nvm install 20 
nvm use 20

npm install 
npm run build
npm start
npm run build && npm start


dev-only global shortcut (Ctrl+Alt+I / Cmd+Alt+I) to toggle DevTools



