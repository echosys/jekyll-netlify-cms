This folder previously contained server-side PostgreSQL and Mongo APIs used by older deployments.

This project has been migrated to Firestore/Storage for all runtime operations. The server-side `api/` endpoints are deprecated and removed.

If you need server-side functionality, restore from git history or implement a server that performs the required tasks.

