I have completed all of your requested features and fixes! Here's a summary of what's been done:

Unsaved Drawn Regions Issue: The problem where unsaved drawn regions were getting lost when moving back to an image has been fixed by directly syncing 

TagEditor
 with the latest live store data instead of a stale cached snapshot list. If you click "yes" to save, the changes are perfectly retained when you go back.
Profile Photo using Region: (Fixed previously, but to confirm) Using a specific drawn region of a photo as a person's profile image now correctly zooms and crops the specified region on the person's avatar (

NodeCard
), using coordinate data embedded in profile_image_ref (?rect=...).
File Export Sequence Naming: Updated the ZIP export logic (

zip.ts
). If the image has a 

date
 label, its exported filename will automatically become [Date]_[Location]_[Tags].[ext]. If there's no date, it will fall back to using your last edited filename within the system. Numbered suffixes (e.g. _1, _2) are appended if necessary to resolve naming conflicts, and your 

.json
 data files and profile_image_ref pointers update accordingly inside the exported backup ZIP.
New User Default Tree Over-permissions: Fixed the behavior where newly created users or default instances were bypassing standard tree restrictions. Both 

App.tsx
 and 

LoginPage.tsx
 now explicitly enforce the filtered tree query based strictly around their allowed_trees access field.
Access Control Not Filtering After Changing: Enforced explicit verification when syncing and switching tree contexts so they properly filter the visible 

listTrees()
 scope for users. Flashing context should be solved via strict user context loading closures on 

App.tsx
 boot.
Removing Tree Access Failed to Work: Found an issue where emptying the user's allowedList permissions sent an empty array [] to the DB. Because Firebase Realtime Database drops and ignores empty array updates, it previously didn't apply. I've switched empty-array assignments to pass null so the database actively wipes the node out like expected!
You can now fully manage roles, easily export smartly named photos based on metadata, and safely bounce between images and edits without losing regional modifications.

Please go ahead and take a look, and let me know if there's any other refinements or questions you have!